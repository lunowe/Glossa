fastapi
uvicorn[standard]
pydantic
pydantic-settings
httpx
motor
pymongo
minio
pyyaml
python-multipart
jinja2>=3.1
# All LLM inference goes through Pydantic AI. The slim package + provider extras
# enable the five supported providers: anthropic, openai (incl. OpenAI-compatible
# endpoints), gemini, bedrock, and vertex. The google/bedrock SDKs are imported
# lazily in glossa/llm/models.py, so trimming an extra here only disables that one
# provider — the rest of Glossa (and the test suite) still runs.
pydantic-ai-slim[openai,anthropic,google,bedrock]>=1.0
mcp>=1.27.1
# Link ingestion: fetch a URL and extract readable main content as markdown.
trafilatura
# Document upload: local, no-API-key document parser (PDF/DOCX/PPTX/…).
# Office formats need LibreOffice; images need ImageMagick; OCR needs Tesseract.
liteparse
